========
SIMPLE TREE
========

build
package.json
src/tree_sitter/parser.h
src/node-types.json
src/srcparser.c
queries/
/

---

(tree
  (file)
  (file)
  (directory
    (directory
      (file)))
  (directory
    (file))
  (directory
    (file))
  (directory)
  (directory))
