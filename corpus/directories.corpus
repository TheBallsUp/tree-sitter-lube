==========================
DIRECTORIES
==========================

/
directory/
directory/nested/
directory/nested/more_nested/
file.h
directory/file.h
directory/nested/file.h

--------------------------

(tree
  (directory)
  (directory)
  (directory
    (directory))
  (directory
    (directory
      (directory)))
  (file)
  (directory
    (file))
  (directory
    (directory
      (file))))
